// Basic Solution

int maxMultiple(int divisor, int bound) {
    return divisor * (bound / divisor);
}

/*--------------------------------------------------*/
/*--------------------------------------------------*/

// One-liner

int maxMultiple(int divisor, int bound) => divisor * (bound / divisor);
